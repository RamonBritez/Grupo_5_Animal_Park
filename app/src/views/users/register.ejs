<!DOCTYPE html>
<html lang="es">
  <%- include("../partials/head") %>

  <body>

    <header class="forms">
      <a href="/"><img src="/image/animalPark/logo_cuadrado_fondo_blanco-mod.png" alt="Home"></a>
  </header>
  <main>
    <main>
      
      <div class="register__container">
        
        <div class="register__container-formTitle">
          <i class="fa-solid fa-circle-user"></i>
          <p class="title">Crear cuenta</p>
        </div>
        
        <form action="/users/register" class="register__container-form" method="post" enctype="multipart/form-data" novalidate>
          
          <div class="form-element">
            <label for="userName">Nombre</label>
            <input type="text" name="userName" id="userName" placeholder="Ej: Cosme" value="<%= locals.old && old.userName %>"/>
            <% if (locals.errors && errors.userName) { %>
              <small class="text-error"><%= errors.userName.msg %> </small>
          <% } %>
          </div>
          
          <div class="form-element">
            <label for="apellido">Apellido</label>
            <input type="text" name="apellido" id="apellido" placeholder="Ej: Fulanito" value="<%= locals.old && old.apellido %>"/>
            <% if (locals.errors && errors.apellido) { %>
              <small class="text-error"><%= errors.apellido.msg %> </small>
          <% } %>
          </div>
          
          <div class="form-element">
            <label for="email">Tu email</label>
            <input type="email" name="email" id="email" placeholder="Ej: cosme@email.com" value="<%= locals.old && old.email %>"/>
            <% if (locals.errors && errors.email) { %>
              <small class="text-error"><%= errors.email.msg %> </small>
          <% } %>
          </div>
          
          <div class="form-element">
            <label for="password">Contraseña</label>
            <input
              type="password" name="password" id="password" value="<%= locals.old && old.password %>" placeholder="¡La contraseña debe contener al menos 8 carácteres!"/>
            <% if (locals.errors && errors.password) { %>
              <small class="text-error"><%= errors.password.msg %> </small>
          <% } %>
          </div>
          
          <div class="form-element">
            <label for="repeatPassword">Vuelve a escribir la contraseña</label>
            <input type="password" name="repeatPassword" id="repeatPassword" value="<%= locals.old && old.repeatPassword %>"/>
            <% if (locals.errors && errors.repeatPassword) { %>
              <small class="text-error"><%= errors.repeatPassword.msg %> </small>
          <% } %>
          </div>

          <div class="input-file">
            <label for="avatar">Agregar Avatar</label>
            <input type="file" id="avatar" name="avatar" accept="image/*">
          </div>


          <div class="form-terms">
            <input type="checkbox" name="terms" id="check" />
            <label for="terms">Acepto los <a href="#" target="_blank">terminos y condiciones</a> </label>
            <br>
            <% if (locals.errors && errors.terms) { %>
              <small class="text-error"><%= errors.terms.msg %> </small>
          <% } %>
          </div>
          
          <button type="submit" class="form-submit">Registrar</button>
        
        </form>
        
        <div class="register__container-separator">
          <div class="line"></div>
          <p>o</p>
          <div class="line"></div>
        </div>
        
        <div class="register__container-network">
          <button> Crear cuenta con Google
          </button>
          <button>Crear cuenta con Facebook</button>
        </div>
        <div class="register__container-separator">
          <div class="line"></div>
          <p>o</p>
          <div class="line"></div>
        </div>
        <div class="existentAccount">
          <p>¿Ya tienes una cuenta?</p>
          <a href="/users/login">Ingresá</a>
        </div>
      </div>
    </main>
    <%- include("../partials/footer")  %> 
  </body>
</html>
